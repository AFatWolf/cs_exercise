function showWaniMessage(){
    alert("I'll bite you! Gabu!");//  fix it
}

function showResult(num){
    var results = Math.random() * 2// fix it
    var message = "Miss!";// fix it
    if (results >= 1) message = "Hit!";
    alert(message);//  fix it
}